{
  "name": "app-agenda",
  "version": "1.0.0",
  "description": "```bash\r -> ProductManager (o ProductDao - Conectar con la DB) \r           -> ProductService (o ProductProvider - Lógica)\r                    -> ProductController (o ProductHandler - Controlador que maneja REQ -> RES)\r                             -> ProductRouter (rutas - url + método + función controladora)\r ```",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "express": "^5.2.1"
  }
}
